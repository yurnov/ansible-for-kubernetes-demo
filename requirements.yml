---
# Install with:
#   ansible-galaxy collection install -r requirements.yml
collections:
  - name: kubernetes.core
    # 6.x is the current stable major (no k8s inventory plugin anymore).
    # Helm v4 support needs >= 6.5.0.
    version: ">=6.0.0"
